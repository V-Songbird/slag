schema_version: "1.1"
name: heldout-contract-section-oriented
description: Held out. A long carrier contract gives the manifest batch sizes in the endpoint's own entry, far past a general batch rule that says otherwise. The agent makes the client's limits match. Oriented snapshot, whose map file routes an endpoint's batch or page size to that endpoint's entry.
tags: [heldout]
context:
  scaffold_script: heldout-contract-section-oriented.sh
