---
max_turns: 25
timeout_seconds: 600
allowed_tools: [Read, Glob, Grep, Skill, Bash]
expected_outcome: Runs the audit script, names the generic utils file, the duplicate format.js files, the missing map file and the unignored dist folder, and creates no files.
---

Before we reorganize anything: how easy is this repository for you to find your way around? List the problems you see, but don't change any files.
