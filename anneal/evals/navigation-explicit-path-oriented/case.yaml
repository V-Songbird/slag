schema_version: "1.1"
name: navigation-explicit-path-oriented
description: Control. The task names the file, so it needs no orientation. The agent adds Norway to the nordic shipping zone. Oriented snapshot, whose extra map file lines the task does not need.
tags: [navigation]
context:
  scaffold_script: explicit-path-oriented.sh
