interface:
  display_name: "anneal-session"
  short_description: "Turn one session's detours into map file changes you approve"
  default_prompt: "Use $anneal-session to audit this session and propose changes to this repository's map file."
policy:
  allow_implicit_invocation: false
