{
  "name": "slag",
  "private": true,
  "description": "Experimental agent plugins, in-tree. The marketplace manifests own every plugin's version, so this file carries none.",
  "license": "MIT",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "check": "node --test"
  }
}
