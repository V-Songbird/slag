{
  "name": "slag",
  "private": true,
  "description": "Experimental agent plugins, in-tree. Each host's manifest owns a plugin's version, so this file carries none.",
  "license": "MIT",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "check": "node --test --test-reporter=spec"
  }
}
