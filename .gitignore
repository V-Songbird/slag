# Benchmark harness (local-only, never published)
.benchmarks/

# Python cache directories
__pycache__/
.pytest_cache/

.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db
node_modules/

# Private notes, and the reference-name blocklist scripts/git-hooks reads
docs/

# Local research skill (drives the private rule-lab harness under docs/)
/.claude/skills/rule-lab/

# Claude — the dev agents, skills and hooks under .claude/ ARE tracked here
/.claude/settings.local.json

# Roadmap and foreman config are local-only, not published
ROADMAP.jsonl
.foreman/

# This repo's dev config ported to other agent CLIs — local-only, not published
.agents/
.codex/
