---
paths: ["src/**", "lib/**"]
---

- Read configuration once, at startup, into one config module. Never read
  `process.env.X` (or the language's equivalent) at the site that uses the
  value — a use-site read hides the dependency and dodges validation.

<!-- generated by jig — prose lever, probabilistic by construction. evidence: reasoned, claude-opus-5 2026-08 -->
