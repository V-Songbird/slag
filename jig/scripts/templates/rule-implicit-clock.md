---
paths: ["src/**", "lib/**"]
---

- Inside logic, take "now" as a parameter or from an injected clock — never
  call the global clock (`Date.now()`, `new Date()`) at the point of use. A
  buried clock read makes the behavior untestable and the bug irreproducible.

<!-- generated by jig — prose lever, probabilistic by construction. evidence: reasoned, claude-opus-5 2026-08 -->
