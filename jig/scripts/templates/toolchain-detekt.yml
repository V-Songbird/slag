# jig:owned — generated from jig's detekt side-file template. Edit this file
# and jig reports it as drifted rather than overwriting your edit.
#
# Point the detekt Gradle extension at it:
#   detekt { config.setFrom(files(".jig/detekt.jig.yml")) }
# jig never runs or downloads detekt itself; if the build does not carry it,
# this file is an ENFORCEMENT GAP until it does.
empty-blocks:
  EmptyCatchBlock:
    active: true
    allowedExceptionNameRegex: "^$"

style:
  ForbiddenComment:
    active: false
