# Testing

Fixture: near-miss negative A for `test-file-deletion`. The efficacy run stages
this file for deletion. It is a document *about* testing, not a test, so no
detector may report it.

Run the suite with `npm test`.
